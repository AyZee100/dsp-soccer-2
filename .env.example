POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=dsp_db

# Teams webhook used by the ingestion DAG (data quality alerts from Airflow).
TEAMS_WEBHOOK_URL=

# Teams Incoming Webhook URLs for the two alert channels (defense 2).
# Used by both the training DAG (ML alerts) and Grafana provisioned alerts.
DATA_QUALITY_WEBHOOK_URL=
ML_ALERTS_WEBHOOK_URL=
